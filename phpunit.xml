<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>

        <!-- The tenant connection is pinned too, not just the landlord one. The
             tests that provision a tenant write a real sqlite file and point this
             connection at its path, so a developer whose .env runs tenants on
             MySQL had those 24 tests handing a file path to MySQL as a schema
             name. Everything here overrides .env, which is the point: the suite
             must not depend on whose machine it runs on. -->
        <env name="TENANT_DB_DRIVER" value="sqlite"/>
        <env name="TENANT_DB_HOST" value="127.0.0.1"/>
        <env name="TENANT_DB_PORT" value="3306"/>
        <env name="TENANT_DB_USERNAME" value=""/>
        <env name="TENANT_DB_PASSWORD" value=""/>
        <env name="TENANT_DATABASE_CONNECTION" value=""/>
        <env name="QUEUES_TENANT_AWARE_BY_DEFAULT" value="false"/>
        <env name="CUSTOM_FIELDS_ENABLED" value="true"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
