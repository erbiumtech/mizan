/.phpunit.cache
/node_modules
/public/hot
/public/storage
/storage/*.key
/storage/pail
/storage/*
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
.claude

# Database dumps pulled from production. Payroll data — never in the repo,
# and this repo is public.
/db

# RoadRunner, fetched per host by `octane:install`. A 62MB binary whose build is
# specific to the server OS and architecture, so it does not belong to the source.
/rr
